{
  "name": "aem-package-rename",
  "version": "1.0.0",
  "description": "A CLI tool to rename site names within AEM import packages",
  "main": "dist/index.js",
  "bin": {
    "aem-package-rename": "./dist/index.js"
  },
  "scripts": {
    "build": "npx tsc",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "aem",
    "adobe",
    "package",
    "rename",
    "cli",
    "content-management"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "adm-zip": "^0.5.16",
    "chalk": "^4.1.2",
    "commander": "^11.1.0",
    "figlet": "^1.8.0"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/figlet": "^1.7.0",
    "@types/node": "^18.19.3",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
